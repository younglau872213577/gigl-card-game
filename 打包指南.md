# GI/GL 卡牌游戏 — Android APK 打包指南

> 代码已完成并通过本地测试（Kivy 2.3.1 + Python 3.12）。
> 打包 APK 需要 Linux 环境（Kivy 官方工具 buildozer 不支持 Windows 原生），本机当前无 WSL/Docker，需先安装 WSL2。

---

## 一、为什么需要 WSL

Kivy 打包 Android 的标准工具 **buildozer** 只能在 Linux/macOS 上运行。你的电脑是 Windows 10（19043，虚拟化已开启），需要先安装 **WSL2（Ubuntu）** 作为 Linux 环境。

---

## 二、三步打包流程

### 第 1 步：安装 WSL2（需管理员 + 重启一次）

1. 以**管理员身份**打开 PowerShell 或 CMD（右键「以管理员身份运行」）
2. 执行：
   ```powershell
   wsl --install
   ```
3. 等待下载 Ubuntu（约 1GB），然后**重启电脑**
4. 重启后 Ubuntu 会自动启动，首次会要求你设置一个**用户名和密码**（记住它，后面 sudo 要用）

### 第 2 步：在 Ubuntu 里运行打包脚本

打开 Ubuntu 终端，执行：
```bash
# 进入项目目录（Windows 桌面在 WSL 里挂载为 /mnt/c/...）
cd "/mnt/c/Users/Administrator/Desktop/生产/GI_GL_Card_Game_Android"

# 运行打包脚本（会自动装 JDK/buildozer 并打包）
bash setup_and_build.sh
```

> 脚本会自动：
> - 安装 JDK17、buildozer、cython 等
> - 把项目复制到 Linux 文件系统（避免 /mnt/c 路径问题）
> - 下载 Android SDK/NDK（首次 2~4GB）+ 编译打包

### 第 3 步：取出 APK

打包完成后，APK 在：
```
~/gigl_card_game/bin/giglcard-0.1.0-arm64-v8a-debug.apk
```
拷贝回 Windows 桌面并安装到手机即可。

---

## 三、耗时预估

| 阶段 | 耗时 |
|------|------|
| 安装 WSL2 | 10~20 分钟（含下载+重启） |
| 首次打包（下载 SDK/NDK + 编译） | 20~60 分钟 |
| 之后重复打包 | 3~5 分钟 |

---

## 四、常见问题

| 问题 | 解决 |
|------|------|
| `wsl --install` 报错 | 确保管理员身份运行；确认 BIOS 已开启虚拟化（本机已开启） |
| sudo 要求密码 | 输入第 1 步设置的 Ubuntu 用户密码 |
| 打包卡在下载 SDK | 网络问题，可挂代理或多次重试（buildozer 支持断点续传） |
| 中文显示方块 | 已内置 NotoSansSC 字体（16MB），会自动加载 |

---

## 五、备选方案（不想装 WSL）

如果不想在本地装 WSL，可用**云端 Linux 打包**（无需重启、零本地安装）：

1. **GitHub Actions**：把本目录推到 GitHub 仓库，用 CI 在云端打包，产物自动上传
2. **Google Colab**：免费 Linux 环境，手动运行 buildozer 打包

需要的话我可以帮你配置 GitHub Actions 的自动打包 workflow。
